'''fuction is created using def and then name
like def square()
we should take care that if we return and we can print after calling function and if we print inside function, we must only call function otherwise it will 2 times'''

# Squaring of a no
def square(n):
    return (n**2)
print(square(5))

